@model DashboardDto
@{
    ViewData["Title"] = "Dashboard";
}

<div class="dashboard">

    <!-- HEADER -->
    <div class="dashboard__header">
        <h1>Dashboard</h1>
    </div>

    <!-- STATS CARDS -->
    <div class="dashboard__stats">

        <div class="dashboard__stat-card">
            <div class="icon">
                <i class="fa-solid fa-user-graduate"></i>
            </div>
            <div class="info">
                <span class="value">@Model.CountStudent</span>
                <span class="label">Total Students</span>
            </div>
        </div>

        <div class="dashboard__stat-card">
            <div class="icon">
                <i class="fa-solid fa-sack-dollar"></i>
            </div>
            <div class="info">
                <span class="value">
                    @Model.RevenueThisMonth.ToString("c0",
                             new System.Globalization.CultureInfo("vi-VN"))
                </span>
                <span class="label">Revenue This Month</span>
            </div>
        </div>

        <div class="dashboard__stat-card">
            <div class="icon">
                <i class="fa-solid fa-user"></i>
            </div>
            <div class="info">
                <span class="value">@Model.CountStaff</span>
                <span class="label">Total Teachers</span>
            </div>
        </div>

        <div class="dashboard__stat-card">
            <div class="icon">
                <i class="fa-solid fa-book"></i>
            </div>
            <div class="info">
                <span class="value">@Model.CountClass</span>
                <span class="label">Total Classes</span>
            </div>
        </div>

    </div>

</div>


