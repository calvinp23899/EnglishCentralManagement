@model PagedResult<StudentDto>

<div class="admin-page admin-teacher">
    <!-- HEADER -->
    <div class="admin-page__header">
        <h1>Student Management</h1>
        <a class="btn-primary" href="@Url.Action("Create", "Student", new { area = "Admin" })">
            + New Student
        </a>
    </div>

    <!-- TOOLBAR -->
    <div class="admin-page__toolbar">
        <input type="text" placeholder="Search student..." />
        <button class="btn-search">Search</button>
    </div>

    <!-- TABLE -->
    <div class="admin-page__table">
        <table>
            <thead>
                <tr>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Gender</th>
                    <th>Status</th>
                    <th width="120">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model == null || !Model.Items.Any())
                {
                    <tr>
                        <td colspan="5" style="text-align:center;">
                            No students found
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var student in Model.Items)
                    {
                        <tr id="student-row-@student.Id">
                            <td>@student.FullName</td>
                            <td>@student.Email</td>
                            <td>@student.PhoneNumber</td>
                            <td>@student.Gender</td>
                            <td>
                                <span class="status status-@student.Status.ToLower().Replace(" ", "-")">
                                    @student.Status
                                </span>
                            </td>
                            <td class="actions">
                                <a class="view"
                                   href="@Url.Action("GetDetailId", "Student", new { area = "Admin", id = student.Id })">
                                    View
                                </a>

                                <a class="edit"
                                   href="@Url.Action("Edit", "Student", new { area = "Admin", id = student.Id })">
                                    Edit
                                </a>

                                <a class="delete"
                                   data-id="@student.Id"
                                   onclick="confirmDeleteStudent(@student.Id)">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                }

            </tbody>

        </table>
    </div>

    <!-- PAGINATION -->
    <div class="admin-page__pagination-wrapper">
        <div class="admin-page__pagination-info">
            Showing <strong>
                @((Model.PageIndex - 1) * Model.PageSize + 1)
                –
                @(Math.Min(Model.PageIndex * Model.PageSize, Model.TotalRecords))
            </strong>
            of
            <strong>@Model.TotalRecords</strong>
            records
        </div>

        <div class="admin-page__pagination">
            <!-- Prev -->
            <button class="btn-prev"
                    onclick="location.href='@Url.Action("Index", new { page = Model.PageIndex - 1 })'"
            @(Model.HasPrevious ? "" : "disabled")>
                ‹ Prev
            </button>

            <!-- Page 1 -->
            <button class="@(Model.PageIndex == 1 ? "active" : "")"
                    onclick="location.href='@Url.Action("Index", new { page = 1 })'">
                1
            </button>

            @{
                int start = Math.Max(2, Model.PageIndex - 2);
                int end = Math.Min(Model.TotalPages - 1, Model.PageIndex + 2);
            }

            <!-- ... trước -->
            @if (start > 2)
            {
                <span class="pagination-dots">...</span>
            }

            <!-- Pages giữa -->
            @for (int i = start; i <= end; i++)
            {
                if (i == Model.PageIndex)
                {
                    <button class="active">@i</button>
                }
                else
                {
                    <button onclick="location.href='@Url.Action("Index", new { page = i })'">
                        @i
                    </button>
                }
            }

            <!-- ... sau -->
            @if (end < Model.TotalPages - 1)
            {
                <span class="pagination-dots">...</span>
            }

            <!-- Page cuối -->
            @if (Model.TotalPages > 1)
            {
                <button class="@(Model.PageIndex == Model.TotalPages ? "active" : "")"
                        onclick="location.href='@Url.Action("Index", new { page = Model.TotalPages })'">
                    @Model.TotalPages
                </button>
            }

            <!-- Next -->
            <button class="btn-next"
                    onclick="location.href='@Url.Action("Index", new { page = Model.PageIndex + 1 })'"
            @(Model.HasNext ? "" : "disabled")>
                Next ›
            </button>
        </div>
    </div>

    <form id="antiForgeryForm" style="display:none;">
        @Html.AntiForgeryToken()
    </form>


</div>
